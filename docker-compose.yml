services:
  portfolio:
    build: ./website
    networks:
      - portfolio

  portfolio-dev:
    build:
      context: ./website
      target: dev
    volumes:
      - ./website:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    networks:
      - portfolio

networks:
  portfolio:
    external: true